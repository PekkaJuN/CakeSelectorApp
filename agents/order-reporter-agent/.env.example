# Database path (relative to project root or absolute)
DATABASE_URL=cake-selector.db

# Optional: override database path for testing
# TEST_DATABASE_URL=test-cake-selector.db

# Optional. Without a key the coach still report.
GEMINI_API_KEY=
